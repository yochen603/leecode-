class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        sum1=0;        sum2=sum(nums)
        for i in range(len(nums)):
            sum2-=nums[i]
            if sum1==sum2:
                return i
            sum1+=nums[i]
        if sum1!=sum2:
            return -1


加减的顺序进行改变